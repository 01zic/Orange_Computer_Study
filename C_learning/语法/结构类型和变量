枚举：
1.枚举的是int 类型，主要是为了给数字变量一个名称
2.主要用于switch，单独设置一个变量遍历

结构：
1.struct 结构名 成员变量；struct 结构名 成员变量 结构变量名 ；struct 成员变量 结构变量名
2.结构变量可以作为参数传进函数，但是这个是值传递，相当于在函数内对结构的复制体进行操作
3.可以在函数内部创建一个本地结构变量，然后返回这个结构变量
4.最好将函数的指针传进结构，然后在函数内对这个指针进行操作，最后返回指针，在函数定义的时候要加*
5.指向结构的指针可以使用p->name表示指向这个结构的成员变量
6.可以创建结构数组，结构的结构，结构中的结构的数组。。。。。。
7.结构可以与typedef一起用，typedef可以设置类型的别名，这个别名就可以使用在参数定义和变量声明的地方

变量：
1.在函数内部声明本地变量的时候，调用函数的时候本地变量存在，离开函数的时候本地变量消失
2.static静态本地变量只会初始化一次，static关键字可以是函数内部的本地变量一直存在，当下次调用这个变量的时候是上次调用结束后的值
3.静态本地变量是特殊的全局变量，它们位于相同的内存域，静态本地变量具有全局的生存期，但是只有局部的作用域
4.返回本地变量的地址是危险的，可能后面的变量会使用这个地址
5.返回全局或者静态本地变量的地址是安全的，最好返回传入的指针
6.尽量不要使用全局变量或者静态本地变量，不要使用全局变量在函数间传递参数和结果，使用全局变量和静态本地变量的函数线程是不安全的


